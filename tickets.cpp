
#include <iostream>
#include <sstream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;


int main(){
    int n;
    cin>>n;
    string t[];
    for(int i=0;i<n;i++){
        string ticketNum;
        cin>>ticketNum;
        int number = atoi(ticketNum.c_str());
        
        int first=0;
        int second=0;
        for(int i=0;i<3;i++){
            first+=ticketNum[i];
        }
        for(int i=3;i<6;i++){
            second+=ticketNum[i];
        }
        
        for(int j=0;j<number;j++){
            string one = another.substr(0,3);
            string two = another.substr(3);
            int first=0;
            int second=0;
        }
        if(ticketNum[])
        cin>>ticketNum;
        int howManyLuckies=0;
        for(int j=000000;j<ticketNum;j++){
            int k = j;

        }
    }
}